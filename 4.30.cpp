#include <iostream>
#include <string>
#include <vector>

using std::cin, std::cout, std::endl;
using std::string;
using std::vector;

void f();

struct temp {
  int mem[10];
};

int main() {
  int x, y, a, b, i;
  temp *p;
  (sizeof(x)) + y;
  sizeof((p->mem)[i]);
  (sizeof(a)) < b;
  cout << sizeof(f()) << endl;


}
