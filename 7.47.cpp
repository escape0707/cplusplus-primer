// because after implicit conversion on Sales_data from string, the object is
// invalid to be used in other calculations, but only in such occassions do we
// need implicit conversion
// so it's better to "explicit" that